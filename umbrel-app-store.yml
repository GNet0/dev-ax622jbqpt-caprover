id: "dev-Ax622jBQPt-caprover" # Choose the ID for your app store. This should contain only alphabets ("a to z") and dashes ("-").
name: "caprover" # Choose the name of your app store. It will show up in the UI as "<name> App Store".